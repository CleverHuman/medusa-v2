<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.medusa.mall.mapper.CouponMapper">

    <resultMap id="CouponResultMap" type="com.medusa.mall.domain.coupon.Coupon">
        <id property="couponId" column="coupon_id"/>
        <result property="name" column="name"/>
        <result property="code" column="code"/>
        <result property="type" column="type"/>
        <result property="amount" column="amount"/>
        <result property="discount" column="discount"/>
        <result property="minPoint" column="min_point"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
        <result property="totalCount" column="total_count"/>
        <result property="usedCount" column="used_count"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <!-- 通过 code 查询 -->
    <select id="selectCouponByCode" resultMap="CouponResultMap">
        SELECT * FROM mall_coupon WHERE code = #{code} LIMIT 1
    </select>

    <!-- 查询单个 -->
    <select id="selectCouponById" resultMap="CouponResultMap">
        SELECT * FROM mall_coupon WHERE coupon_id = #{id}
    </select>

    <!-- 查询列表 -->
    <select id="selectCouponList" resultMap="CouponResultMap">
        SELECT * FROM mall_coupon
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="code != null and code != ''">
                AND code LIKE CONCAT('%', #{code}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <!-- 插入 -->
    <insert id="insertCoupon" useGeneratedKeys="true" keyProperty="couponId">
        INSERT INTO mall_coupon
        (name, code, type, amount, discount, min_point, start_time, end_time,
         total_count, used_count, status, create_by, create_time, update_by, update_time, remark)
        VALUES
            (#{name}, #{code}, #{type}, #{amount}, #{discount}, #{minPoint}, #{startTime}, #{endTime},
             #{totalCount}, #{usedCount}, #{status}, #{createBy}, #{createTime}, #{updateBy}, #{updateTime}, #{remark})
    </insert>

    <!-- 更新 -->
    <update id="updateCoupon">
        UPDATE mall_coupon
        SET name = #{name},
            code = #{code},
            type = #{type},
            amount = #{amount},
            discount = #{discount},
            min_point = #{minPoint},
            start_time = #{startTime},
            end_time = #{endTime},
            total_count = #{totalCount},
            used_count = #{usedCount},
            status = #{status},
            update_by = #{updateBy},
            update_time = #{updateTime},
            remark = #{remark}
        WHERE coupon_id = #{couponId}
    </update>

    <!-- 删除 -->
    <delete id="deleteCouponById">
        DELETE FROM mall_coupon WHERE coupon_id = #{id}
    </delete>

    <delete id="deleteCouponByIds">
        DELETE FROM mall_coupon WHERE coupon_id IN
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 增加优惠券使用次数 -->
    <update id="incrementUsedCount">
        UPDATE mall_coupon 
        SET used_count = used_count + 1,
            update_time = NOW()
        WHERE coupon_id = #{couponId}
    </update>

    <!-- 减少优惠券使用次数 -->
    <update id="decrementUsedCount">
        UPDATE mall_coupon 
        SET used_count = GREATEST(used_count - 1, 0),
            update_time = NOW()
        WHERE coupon_id = #{couponId}
    </update>

</mapper>
