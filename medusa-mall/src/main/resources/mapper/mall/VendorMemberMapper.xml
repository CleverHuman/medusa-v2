<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.medusa.mall.mapper.VendorMemberMapper">

    <resultMap id="VendorMemberResult" type="com.medusa.mall.domain.vendor.VendorMember">
        <id     property="id"            column="id"/>
        <result property="username"      column="username"/>
        <result property="passwordHash"  column="password_hash"/>
        <result property="email"         column="email"/>
        <result property="phone"         column="phone"/>
        <result property="status"        column="status"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="createTime"    column="create_time"/>
        <result property="updateTime"    column="update_time"/>
        <result property="remark"        column="remark"/>
    </resultMap>

    <sql id="baseColumns">
        id, username, password_hash, email, phone, status,
        last_login_time, create_time, update_time, remark
    </sql>

    <select id="selectById" parameterType="long" resultMap="VendorMemberResult">
        select <include refid="baseColumns"/> from mall_vendor_member where id = #{id}
    </select>

    <select id="selectByUsername" parameterType="string" resultMap="VendorMemberResult">
        select <include refid="baseColumns"/> from mall_vendor_member where username = #{username}
    </select>

    <select id="selectVendorMemberList" parameterType="com.medusa.mall.domain.vendor.VendorMember" resultMap="VendorMemberResult">
        select <include refid="baseColumns"/> from mall_vendor_member
        <where>
            <if test="username != null and username != ''">
                and username like concat('%', #{username}, '%')
            </if>
            <if test="email != null and email != ''">
                and email like concat('%', #{email}, '%')
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>

    <insert id="insertVendorMember" parameterType="com.medusa.mall.domain.vendor.VendorMember" useGeneratedKeys="true" keyProperty="id">
        insert into mall_vendor_member
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="username != null">username,</if>
            <if test="passwordHash != null">password_hash,</if>
            <if test="email != null">email,</if>
            <if test="phone != null">phone,</if>
            <if test="status != null">status,</if>
            <if test="lastLoginTime != null">last_login_time,</if>
            <if test="remark != null">remark,</if>
            create_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="username != null">#{username},</if>
            <if test="passwordHash != null">#{passwordHash},</if>
            <if test="email != null">#{email},</if>
            <if test="phone != null">#{phone},</if>
            <if test="status != null">#{status},</if>
            <if test="lastLoginTime != null">#{lastLoginTime},</if>
            <if test="remark != null">#{remark},</if>
            sysdate()
        </trim>
    </insert>

    <update id="updateVendorMember" parameterType="com.medusa.mall.domain.vendor.VendorMember">
        update mall_vendor_member
        <set>
            <if test="passwordHash != null">password_hash = #{passwordHash},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="status != null">status = #{status},</if>
            <if test="lastLoginTime != null">last_login_time = #{lastLoginTime},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate(),
            update_by = #{updateBy}
        </set>
        where id = #{id}
    </update>

</mapper>

